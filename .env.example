# Client-only (exposed to browser)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Server-only (never expose)
OPENROUTER_API_KEY=

# Gradium TTS/STT — get key from https://gradium.ai (format gd_... or gsk_...)
# GRADIUM_API_KEY=gd_or_gsk_your_key_here
# TTS: set GRADIUM_TTS_WS_URL (wss://) for WebSocket. Use "us" in North America, "eu" in Europe.
# GRADIUM_TTS_WS_URL=wss://us.api.gradium.ai/api/speech/tts
# GRADIUM_TTS_URL=https://us.api.gradium.ai/api/post/speech/tts
# GRADIUM_STT_URL= (Gradium STT is WebSocket-only; leave unset for browser fallback)
# Restart the dev server after changing these.
